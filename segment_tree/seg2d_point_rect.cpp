#include "seg2d_point_rect.hpp"
#define pb push_back
#define all(v) begin(v), end(v)

void solve(){
    // SegTree2D st(n, m, v);
    // st.update(x, y, val);
    // st.query(x1, x2, y1, y2);
}

signed main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int t = 1;
    // cin >> t;
    while (t--){
        solve();
    }
    return 0;
}