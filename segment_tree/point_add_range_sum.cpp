#include "point_add_range_sum.hpp"
#define pb push_back
#define all(v) begin(v), end(v)

void solve(){
    // SegTree st(n, v);
    // st.upd(i, val);
    // st.query(l, r);
}

signed main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int t = 1;
    // cin >> t;
    while (t--){
        solve();
    }
    return 0;
}